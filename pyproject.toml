[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pocket_ic"
version = "0.0.0"
authors = [
  { name = "The Internet Computer Project Developers", email = "dept-testing_&_verification@dfinity.org" },
]
description = "A canister testing library"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: Apache Software License",
]

[project.urls]
"Homepage" = "https://github.com/dfinity/pocketic-py"
